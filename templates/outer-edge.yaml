clusterEdges: {{ clusterEdges|tojson }}

proxy:
  replicaCount: {{ config.outerEdge.proxy.replicaCount }}
  resources:
    requests:
      cpu: 0.25
      memory: 1Gi
    limits:
      cpu: 0.25
      memory: 1Gi